type Player
  @model
  @key(fields: ["username"])
  @auth(rules: [
  { allow: private, operations: [read]}
  { allow: private, provider: iam, operations: [read, create, update, delete]}
  ]) {
  id: ID!
  username: String!
  cognitoID: String!
  name: String!
  email: AWSEmail!
}
